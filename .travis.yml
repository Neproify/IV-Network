language: cpp

matrix:
  include:
    - compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-5
            - gcc-5
            - libc6-dev-i386

install:
  - export CXX="g++-5" CC="gcc-5";

script: make